# 改动记录（自用版）

本仓库从 anareaty/pretty-properties 2.0.12 分出，**独立发展，不再同步上游**。
所有改动都记在这里，按时间倒序。改代码时同步更新本文件。

## 2026-09-22

### 中文界面
- 新增 `src/localization/locales/zh.ts`（简体），`localization.ts` 里同时注册 `zh` 和 `zh-TW`。
- 以后加新的界面文字，要同时加进 `en.ts`、`ru.ts`、`zh.ts` 三个表（键名一致）。

### 横幅
| 设置项 | 默认 | 改在哪 |
|---|---|---|
| 横幅渐隐起点（%） | 25 | `updateStyles.ts` 的 `buildFadeGradient`；渐变用慢→快→慢曲线 |
| 横幅圆角（px） | 10 | `styles.css` 的 `.pp-banner img` 用 `--banner-radius` |
| 默认横幅 | 空 | `updateBanners.ts` 的 `renderBanner` 开头回退；`updateElements.ts` 放行没有属性的笔记 |
| 拖动横幅调整取景 | 开 | `updateBanners.ts` 的 `makeBannerDraggable`，松手写入 `banner_position` |

### 选图弹窗
- 选择封面图片的缩略图统一 130px 高、宽度按图片自身比例（横图宽、竖图窄，最宽 240px 后裁切），下方显示文件名（`styles.css` 的 `.image-suggestion-item.cover`，`localImageSuggestModal.ts`）。

### 窄窗口
- 窗口宽度不足 600px 时，属性名列从 9em 缩到 6em，避免标签药丸被逐字掰断；标签药丸改为整颗换行、不在单词中间断开，超过整行宽度的才省略号（都在 `styles.css` 的 `@container (width < 600px)` 块）。

### 封面
| 设置项 | 默认 | 改在哪 |
|---|---|---|
| 封面圆角（px） | 0 | `styles.css` 的 `.pp-cover-image` 用 `--cover-radius` |
| 默认封面 | 空 | `updateCovers.ts` 的 `renderCover` 回退；`coverMenu.ts` 无属性时也能右键 |
| 拖动 / 缩放封面调整取景 | 开 | `updateCovers.ts` 的 `makeCoverAdjustable`：拖动改 `cover_x` / `cover_y`（0–100），Ctrl+滚轮改 `cover_zoom`（1–4）；右键菜单「重置封面取景」清掉三个属性。图片外面加了一层 `.pp-cover-frame` 裁切框（`imageUtils.ts` 的 `createImageWrapper`），圆角也挪到框上。网页复现同一裁切用：`object-fit: cover; object-position: X% Y%; transform: scale(Z); transform-origin: X% Y%`，外层 `overflow: hidden` |
| 封面效果：透明度 / 马赛克 | 100 / 0 | 右键封面 →「封面效果」：不透明度滑块写 `cover_opacity`（0–100）；马赛克滑块写 `cover_mosaic`（横向格子数，0 = 关，最大 64）。马赛克是把封面图本身在画布上缩到 N 格宽再用最近邻放大（`utils/coverMosaics.ts` 的 `pixelateImage`，img 加 `.pp-pixelated` = `image-rendering: pixelated`），不配色。外链图片画布受限时不处理。默认值自动从属性删掉。模糊功能已去掉 |
| 宽版（16:9）封面宽度 | 320 | 形状 `wide-cover` / `wide-contain`：`coverShapeSuggestModal.ts`、`updateCovers.ts` 形状列表、`styles.css` |

所有新设置字段定义在 `src/settings/settings.ts`；设置页在 `src/settings/bannerSettings.ts` 和 `coverSettings.ts`（每个文件里有两套渲染代码，新旧 API 各一套，两处都要改）。

## 构建与安装
```
npm run build
```
把 `main.js`、`styles.css`、`manifest.json` 复制到库的 `.obsidian/plugins/pretty-properties/`，然后 Obsidian 按 Ctrl+R 重载。

**别在 Obsidian 社区插件里点这个插件的更新**，会被商店版本覆盖。
